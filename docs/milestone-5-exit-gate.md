# Milestone 5 literal exit-gate audit — production UI and real-state migration

Date: 2026-09-25. Branch: `web/milestone-5-production-ui` against `master`.

## Verdict and evidence standard

**BLOCKED — final-head GitHub CI and dependency review could not run.** This document does not call the milestone passed or merged. A requirement is marked PASS below only when its production implementation and a repeatable check or inspected artifact are named. The exact missing proof is listed at the end.

The audit uses the literal Milestone-5 section of _University Planner — Implementation Roadmap_, the master specification's UI and Visual Design Guidelines, UX/information architecture, responsive and accessibility rules, and frozen MVP Interface Contract. ADRs [0007](decisions/0007-production-ui-shell-and-route-state.md), [0008](decisions/0008-information-views-and-raw-capture-boundary.md), and [0009](decisions/0009-planner-presentation-boundary.md) record release-boundary choices. The [Milestone-4 exit gate](milestone-4-exit-gate.md) remains merged in `master`; this branch keeps its Planner Service tests, migrations and package boundaries.

## Foundation and shell

| Literal requirement                                                                                              | Production implementation                                                                                                                    | Proof                                                                                                                                         | Status |
| ---------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| IBM Plex Sans and Mono                                                                                           | `apps/web/src/app/layout.tsx`, local `@fontsource` packages and `styles.css`                                                                 | Production build; controlled captures embed the installed font files                                                                          | PASS   |
| One semantic light/dark token system, course/status colours, spacing, type, radius, border, elevation and layers | `styles.css` root and dark-theme variables; `planner-primitives.tsx`, `course-color.ts`                                                      | `ui-foundation.test.mjs`, `quality-gate.spec.ts`; inspected light/dark captures. Today capture helper now loads the real course-colour mapper | PASS   |
| Motion, focus, breakpoints and layout dimensions                                                                 | `styles.css` defines 228 px sidebar, 64 px rail, 56 px top bar, motion/easing, 2 px focus and 900/640 px transformations                     | `quality-gate.spec.ts` checks 1440/1024/768/640/390 widths in both themes, focus, reduced motion and 44 px targets                            | PASS   |
| Lucide and restrained Motion; no default shadcn styling or Anime.js                                              | `app-shell.tsx` and owned primitives; pinned runtime dependencies                                                                            | Dependency/source audit, package manifest and browser captures                                                                                | PASS   |
| Anti-slop visual doctrine                                                                                        | Restrained shell, rows, timeline and calendar surfaces; no hero, KPI-card strip, glass, decorative chart, timer or AI glow                   | Human comparison with `approved-preview` and `approved-designs`; [visual QA](milestone-5-visual-qa.md)                                        | PASS   |
| Protected AppShell and signed-out entry                                                                          | `(planner)/layout.tsx`, `app-shell.tsx`, `/sign-in`, `server/auth.ts`                                                                        | `bootstrap.spec.ts`; guarded authenticated Chromium proof in `ui-authenticated.browser.mjs`                                                   | PASS   |
| Navigation and route/history model                                                                               | Eight primary/secondary routes, mobile bottom navigation, `assessment`, `course`, `panel`, `scenario`, `conflict`, `date`, `day` query state | ADR 0007; `ui-foundation`, `information-ui`, `secondary-ui`, `presentation-ui` tests and E2E Back/Forward tests                               | PASS   |

## Production screen order and real data authority

Every route below is a protected App Router route. Server Components call the listed authenticated application read service; components receive typed presentation models. Synthetic data exists only in tests and the approved preview.

| Roadmap screen family             | Route/component and canonical source                                                                                            | Reproducible evidence and later mutation boundary                                                                                                                                                               | Status |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 1. AppShell and primitives        | `(planner)/layout.tsx`, `app-shell.tsx`, `planner-primitives.tsx`                                                               | `ui-foundation.test.mjs`, `quality-gate.spec.ts`; no fixture route data                                                                                                                                         | PASS   |
| 2. Today                          | `/today`, `today-view.tsx` → `plannerViews.today`                                                                               | `planner-reads.test.js`, `today-ui.test.mjs`, `today.spec.ts`, live PostgreSQL/Chromium. Complete/Adjust defer to M7. Overnight objects are clipped to the selected local day, with a live regression assertion | PASS   |
| 3. Week                           | `/week`, `week-view.tsx`, pure `week-layout.ts` → `plannerViews.week`                                                           | `planner-reads`, `week-ui`, `week.spec.ts`, live PostgreSQL/Chromium; semantic seven-day list and mobile agenda. Drag/replan defer to later milestones                                                          | PASS   |
| 4. Upcoming and Assessment Detail | `/upcoming`, `upcoming-view.tsx` → `informationViews.upcoming`                                                                  | `information-reads`, `information-ui`, `information.spec.ts`, live canonical route. Authorized `assessment` query, mobile full detail and Back restoration                                                      | PASS   |
| 5. Inbox and Quick Capture        | `/inbox`, `inbox-view.tsx`, `quick-capture.tsx` → `informationViews.inbox`; raw capture → existing `inboxItems.capture` service | `information-reads`, `information-ui`, authenticated Chromium; success requires persisted ID. Interpretation/resolution defer to M6                                                                             | PASS   |
| 6. Course Detail                  | `/courses`, `courses-view.tsx` → `secondaryViews.courses`                                                                       | `secondary-reads`, `secondary-ui`, `secondary.spec.ts`, live canonical route; unauthorized IDs unavailable. Editing defers to M6                                                                                | PASS   |
| 7. Calendar and Availability      | `/availability`, `availability-view.tsx` → `secondaryViews.availability`                                                        | `secondary-reads`, `secondary-ui`, `secondary.spec.ts`, live canonical route; shared recurrence/timezone expansion. Rule CRUD defers to M6                                                                      | PASS   |
| 8. Settings and Integrations      | `/settings`, `/integrations` → `secondaryViews.settings/integrations`                                                           | `secondary-reads`, `secondary-ui`, authenticated Chromium. Only recorded account/provider metadata; preference CRUD M6, Google sync M9, LMS M14                                                                 | PASS   |
| 9. Planner panel                  | `app-shell.tsx`, `planner-surface.tsx` → narrow `plannerPresentation` projection from Today read                                | `planner-presentation.test.js`, `presentation-ui.test.mjs`, `presentation.spec.ts`; no assistant execution. Language interpretation defers to M10                                                               | PASS   |
| 10. Scenario and Conflict         | URL-driven `planner-surface.tsx` → recorded plan/risk projection                                                                | `presentation-ui` and E2E; unprepared comparison and disabled Apply are explicit. Trusted scenario/tradeoff/Apply defer to M8                                                                                   | PASS   |
| 11. Onboarding                    | `/onboarding`, `onboarding-view.tsx` → `secondaryViews.onboarding`                                                              | `secondary-reads`, `presentation-ui`, authenticated Chromium. Step URLs never claim canonical setup; mutations defer to M6                                                                                      | PASS   |
| 12. Mobile transformations        | Shell bottom nav; Week agenda; detail and Planner sheets                                                                        | Five-width/two-theme Chromium matrix, route Back tests, mobile captures                                                                                                                                         | PASS   |

## Fixture, architecture and state proof

| Requirement                                                           | Evidence                                                                                                                                                                                                                                                                                                                         | Status |
| --------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| No critical route imports preview/test schedules or copied demo facts | `scripts/check-package-boundaries.mjs` checks every production web source; negative import tests; source search. Approved fixture remains under `prototypes/approved-preview`, `tests`, and `docs/regression-reference` only                                                                                                     | PASS   |
| UI → application/read service → scoped repository/database            | Protected server pages call `plannerViews`, `informationViews`, `secondaryViews`; `check:boundaries` rejects direct Prisma/repository and client server-internal imports                                                                                                                                                         | PASS   |
| No client-supplied identity or planner/date logic in React            | `requireActor` derives the Auth.js user; query IDs/dates validated in services; shared time/recurrence and pure Week layout tests cover DST, overlap and local boundaries                                                                                                                                                        | PASS   |
| Real canonical state reaches real production components               | `tests/database/ui-real-state.integration.mjs` reads nine screen models on a disposable PostgreSQL database and renders their production components; it creates a separate plan-ready actor through canonical repositories, runs the real Planner Service and proves CURRENT Today/Week with generated sessions                  | PASS   |
| Real protected browser routes reach canonical state                   | `tests/database/ui-authenticated.browser.mjs` uses a test-only Auth.js signed JWT for synthetic persisted users. The unchanged protected Next.js routes verify the database actor, render nine routes, reject signed-out access, and show CURRENT and FAILED plan contexts. No bypass or fixture import is bundled in production | PASS   |
| Milestone 4 remains intact                                            | M4 exit gate merged at `6c4b70d`; unchanged planner scenario/property suites and authoritative unit tests remain in `pnpm verify`; live M5 proof invokes the actual M4 Planner Service                                                                                                                                           | PASS   |

The disposable database was `up_test_m5_ui_gate_20260925` on expiring schema-only Neon branch `br-royal-wave-b4tzqior` in project `purple-tooth-70442528` (expiry 2026-09-27 04:00 UTC). Nine migrations deployed from zero; the guarded synthetic semester seed and integrity assertion passed. The old seed predates the M4 explicit sleep/capability contract, so a separate test-only plan-ready user was created for the successful PlannerRun proof. An attempted run on the old fixture safely recorded FAILED/INVALID_OUTPUT without publishing generated sessions; the authenticated browser correctly displayed the failure. No private university data or credential is committed.

The reproducible guarded commands are `pnpm db:ui:real-state:verify` with `APP_ENV=test`, `CONFIRM_M5_UI_DATABASE=RUN_M5_REAL_STATE_PROOF` and `M5_UI_TEST_DATABASE_URL` pointing to a direct, disposable `up_test_m5_ui_gate_*` database; and `pnpm db:ui:browser:verify` with the same URL/confirmation, a localhost `M5_UI_BROWSER_URL`, and the same test-only `AUTH_SECRET` used by the local Next.js server. That server receives the disposable `DATABASE_URL` and dummy Google client metadata; no Google OAuth request is made. Optional `M5_UI_CAPTURE_DIR` and `M5_UI_AUTH_CAPTURE_DIR` write review screenshots. The browser script signs synthetic actor tokens only inside the guarded test process; the production Auth.js/JWT and database actor checks run unchanged. Seed and migration commands retain their own repository guards.

## Accessibility, responsive and visual evidence

| Gate                                                                                  | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Status |
| ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Keyboard navigation, labelled landmarks/controls, dialog naming and focus restoration | `ui-foundation.test.mjs`, `information-ui.test.mjs`, `presentation-ui.test.mjs`, `quality-gate.spec.ts`; modal focus/Escape/Back implementations in `app-shell.tsx`, close components                                                                                                                                                                                                                                                                                                                                                                      | PASS   |
| Visible 2 px focus, 44×44 mobile controls, non-colour-only status/course identity     | `styles.css`, course/status primitives, `quality-gate.spec.ts` focus and target audit; inspected real-state captures                                                                                                                                                                                                                                                                                                                                                                                                                                       | PASS   |
| 200% zoom, reduced motion, readable Week alternative                                  | `quality-gate.spec.ts` equivalent viewport and semantic seven-day list; `week.spec.ts`; reduced-motion CSS and Motion hook                                                                                                                                                                                                                                                                                                                                                                                                                                 | PASS   |
| Desktop/laptop/tablet/mobile, light/dark parity                                       | `quality-gate.spec.ts`: 12 screen families × 5 widths × 2 themes, plus controlled captures. Authenticated Chromium confirms mobile Today, real-state desktop Today/Week and theme persistence after reload                                                                                                                                                                                                                                                                                                                                                 | PASS   |
| Approved visual hierarchy and interaction contract                                    | Compared [production captures](regression-reference/production-qa) and [real-state captures](regression-reference/production-real-state) with [approved preview references](regression-reference/approved-preview) and [approved designs](regression-reference/approved-designs). 228 px shell, 56 px top bar, restrained current/next, fixed-versus-work distinction, seven-column Week and mobile agenda remain recognizable. Data density varies with canonical facts; real-state captures intentionally do not fill empty space with fixture schedules | PASS   |

The authenticated Chromium screenshots in `regression-reference/production-authenticated` were taken from a local **development** server and contain Next.js's development badge. That badge is tooling chrome, not an application element; the production component captures and optimized build establish the app appearance without it. Today/Week real-state captures exposed and prompted two corrections: overnight objects now show only their local-day visible segment, and the Today screenshot renderer now uses the actual course-colour mapper. The approved preview remains separately tested and unchanged.

## Verification and remaining gate

Local `pnpm verify` passed after the production fixes on the staged implementation state: formatting, lint, 123-source-file package boundaries, Prisma generation/validation, strict typecheck, **150/150 unit**, **66/66 integration**, optimized Next.js build and **28/28 Chromium E2E** tests, including the approved-preview regression. Guarded live model-to-component proof passed **1/1**; guarded authenticated browser proof passed **1/1**. Direct reruns of the dedicated planner suites passed **13/13 scenarios** and **2/2 properties**. Local audit implementation commit: `6ed271c2d4d9867752d67264b05d859370abfbc7`. Direct formatting, lint, boundaries and strict typecheck also passed after the audit-only files were added.

**Missing proof and operational block:** after the local full suite, the attached shell changed from the repository-pinned Node 24.21.0/pnpm 12.5.1 pair to Node 24.19.0/pnpm 11.19.0. A final `pnpm verify`/`pnpm check:dependencies` retry refused to run because that pnpm tried to replace `node_modules`; no dependency installation was allowed. The current local commit could not be pushed: `git push` could not connect to `github.com:443`. The GitHub connector rejected a write attempt with “MCP tool call requires approval, but approval policy is never,” and a network permission request returned no grant. Therefore no final-head GitHub CI run or dependency review exists, and no PR was opened. These are unproved gate items, not presumed passes.

**Exact next action when the environment is restored:** use Node 24.21.0 and pnpm 12.5.1; rerun `pnpm verify`, `pnpm check:dependencies` and the guarded database/browser proof as appropriate; push this branch; open a clearly provisional PR to trigger GitHub CI and dependency review; require both checks on the final PR head; then, and only then, record `Milestone 5 — GATE PASSED, awaiting merge` and make the PR ready. Milestone 5 remains **BLOCKED**, with Milestones 6+ deferred and no merge.
